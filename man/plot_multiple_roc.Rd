% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization_roc.R
\name{plot_multiple_roc}
\alias{plot_multiple_roc}
\title{Plot Multiple ROC Curves}
\usage{
plot_multiple_roc(
  roc_object_list,
  plot_title = "ROC Curves",
  color_palette = NULL,
  smooth_curves = TRUE,
  legend_position_x = 0.55,
  legend_position_y = 0.6,
  line_width = 1.5
)
}
\arguments{
\item{roc_object_list}{A named list of ROC curve objects (class `roc` from
`pROC` package). Names are used in the legend.}

\item{plot_title}{Character string for the main title of the plot.}

\item{color_palette}{A vector of color strings to use for plotting the ROC
curves. If `NULL` (default), `RColorBrewer::brewer.pal` "Dark2" is used.
Length should be at least `length(roc_object_list)`.}

\item{smooth_curves}{Logical, if `TRUE` (default), ROC curves are smoothed
using `pROC::smooth`.}

\item{legend_position_x}{Numeric, x-coordinate for the top-left of the
legend text block (range 0-1). Default 0.55.}

\item{legend_position_y}{Numeric, y-coordinate for the top-left of the
legend text block (range 0-1). Default 0.60.}

\item{line_width}{Numeric, line width for ROC curves. Default 1.5.}
}
\value{
Invisibly returns `NULL`. The function is called for its side
  effect of plotting.
}
\description{
Plots one or more ROC curve objects (from the `pROC` package) on the same
graph. Curves can be smoothed, and AUC values are displayed.
}
\examples{
if (requireNamespace("pROC", quietly = TRUE) &&
    requireNamespace("RColorBrewer", quietly = TRUE)) {
  # Create dummy ROC objects
  set.seed(123)
  roc1 <- pROC::roc(response = rbinom(50,1,0.5), predictor = rnorm(50), quiet=TRUE)
  roc2 <- pROC::roc(response = rbinom(50,1,0.7), predictor = rnorm(50,0.5), quiet=TRUE)
  roc_list <- list(MethodA = roc1, MethodB = roc2)

  # Plot them
  # plot_multiple_roc(roc_list, plot_title = "Comparison of Methods")
}
}
