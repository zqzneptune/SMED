% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ElutionScore.R
\name{ElutionScore}
\alias{ElutionScore}
\title{Aggregate Elution-based PPI Scores}
\usage{
ElutionScore(
  mRaw,
  n_fracs = 2,
  top_ppi = 20000,
  methods = c("CoApex", "DICE", "MI", "PCCN", "WCC"),
  ...
)
}
\arguments{
\item{mRaw}{A numeric matrix of protein elution profiles (proteins as rows, fractions as columns).}

\item{n_fracs}{Minimum number of fractions a protein must be present in. Default is 2.}

\item{top_ppi}{Maximum number of PPIs to return for each method (if applicable). Default is 20000.}

\item{methods}{Character vector specifying which scoring methods to include. 
Available options: "CoApex", "DICE", "MI", "PCCN", "WCC". 
Default includes all five.}

\item{...}{Additional parameters passed to individual scoring functions (e.g., `seed`).}
}
\value{
A data frame containing the aggregated scores for each PPI.
}
\description{
This function runs selected scoring methods (from CoApex, DICE, MI, PCCN, WCC)
on a raw elution matrix and merges the results into a single data frame.
}
\examples{
data(dummy_elution_matrix)
# Calculate only MI and PCCN scores
scores <- ElutionScore(dummy_elution_matrix, n_fracs = 1, top_ppi = 100, 
                       methods = c("MI", "PCCN"))
head(scores)
}
